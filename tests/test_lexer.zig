const std = @import("std");
const testing = std.testing;

// Simple lexer test that doesn't require external modules
test "basic lexer functionality" {
    // Test that the build system is working
    try testing.expect(true);
}